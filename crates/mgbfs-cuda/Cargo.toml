[package]
name = "mgbfs-cuda"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[features]
cuda = []
library-owner = []

[dependencies]
mgbfs-core = { path = "../mgbfs-core" }

[[example]]
name = "generation_bench"
required-features = ["cuda"]
