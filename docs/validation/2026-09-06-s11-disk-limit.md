# S11 profile panel v1: platform disk failure

Kaggle `trydotatwo/mgbfs-distributed-bench-s11`, v1, source
`e3095c1c22b629003f0121bedf8d056e9f8aa1aa`, script version 347578599.
The authoritative browser Logs page reports failure after 1431.6 seconds:
"Your notebook tried to use more disk space than is available."
Output is 0 B; CLI output/files return no artifacts. No usable timing or
correctness result is inferred, and the exact failing stage is unknown.

Next run selects `permutation_u8` only for archival: lossless 11-byte
permutations instead of 121-byte permutation matrices. Search states remain
`matrix_u8` in all twelve profiles. This preserves all state information but
changes the output encoding, so it must be recorded explicitly. The fixed
per-rank file extent formula falls from `39,916,800 * 137 + 64 MiB` to
`39,916,800 * 27 + 64 MiB`; both ranks have their own extents. This reduction
does not prove which directory caused the original platform failure.
